config DEDUPFS
	tristate "Dedup fs support"
	help
	  DedupFS is a filesystem based on dedupfs with block deduplication.

	  To compile this file system support as a module, choose M here: the
	  module will be called dedupfs.

	  If unsure, say Y.
